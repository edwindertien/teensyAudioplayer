#pragma once

// ── UART to Teensy (connect when ready — leave commented until wired) ──
// #define TEENSY_UART   Serial1
// #define TEENSY_TX     43      // ESP32 TX → Teensy RX pin
// #define TEENSY_RX     44      // ESP32 RX ← Teensy TX pin
// #define TEENSY_BAUD   115200
